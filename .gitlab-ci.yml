image: ubuntu:latest

before_script:
  - apt-get update
  - apt-get -y upgrade
  - apt-get -y install make gcc bison flex python cvs pylint cppcheck shellcheck

test:
  script:
  - echo "Running as ${USER:-root}"
  - make check
